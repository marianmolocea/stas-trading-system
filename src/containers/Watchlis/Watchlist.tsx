import React from 'react';

import styles from './Watchlist.module.css';
import { Row } from 'antd';
import TickerInput from '../../components/TickerInput/TickerInput';

const Watchlist = () => {
  return (
    <Row className={styles.Watchlist}>
      Watchlist
    </Row>
  );
};

export default Watchlist;
