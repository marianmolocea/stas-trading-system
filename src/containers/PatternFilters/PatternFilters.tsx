import React from 'react';

import styles from './PatternFilters.module.css';
import { Row } from 'antd';

const PatternFilters = () => {
  return (
    <Row className={styles.PatternFilters}>
      Pattern Filters
    </Row>
  );
};

export default PatternFilters;
